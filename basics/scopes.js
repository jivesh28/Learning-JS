
if(1)
{
    let a =10;
    const b=1;
    var c=10;
}
//console.log(a);
console.log(c); // var doesnot follow scope rule , so it should not be used
// in browser and in ide the global scopes are different

// nesteed scopes 

// in javascript variables can hold functions too
